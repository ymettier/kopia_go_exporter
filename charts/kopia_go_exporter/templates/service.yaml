apiVersion: v1
kind: Service
metadata:
  name: {{ include "kopia_go_exporter.fullname" . }}
  labels:
    {{- include "kopia_go_exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kopia_go_exporter.selectorLabels" . | nindent 4 }}
